# Everything is optional. With no key the example runs offline with a deterministic stand-in model.
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-2.5-flash"

# Optional text-only alternative: any OpenAI-compatible endpoint (e.g. Nebius Token Factory, Groq)
OPENAI_COMPAT_BASE_URL=""
OPENAI_COMPAT_API_KEY=""
OPENAI_COMPAT_MODEL="meta-llama/Llama-3.3-70B-Instruct"
